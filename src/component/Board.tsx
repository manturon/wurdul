import React from "react";

/**
 * Table of rows for each guess played in the game, along with a row of the current
 * user (uncommitted) input, and empty rows for the available guesses left.
 */
export default function Board() {
  return <></>;
};
