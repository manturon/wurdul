import React from "react";

/**
 * Input that fires events when the input text has a corresponding transcript,
 * and when the user commits their input (presses Enter or clicks on the button).
 */
export default function Input() {
  return <></>;
}
